DROP FUNCTION IF EXISTS fn_NFotografias
GO

CREATE FUNCTION fn_NFotografias(@NOMBREFOTOGRAFO VARCHAR(60))
RETURNS INT
AS
BEGIN 
	DECLARE @N INT

	SELECT @N = COUNT(*) FROM FOTOGRAFIA WHERE COD_FOTOGRAFO = (SELECT CODIGO FROM FOTOGRAFO WHERE NOME = @NOMBREFOTOGRAFO)

	RETURN @N
END
GO